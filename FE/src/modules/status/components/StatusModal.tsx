const StatusModal = ({
    isOpen,
    handleClose,
    mode,
    status,
}: {
    isOpen: boolean;
    handleClose: () => void;
    mode: "add" | "edit";
    status: any;
}) => {

    

  return <div>
    
  </div>;
};

export default StatusModal;
